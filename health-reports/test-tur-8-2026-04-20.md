# Tur 8 — Haftalık Kapsamlı Test (2026-04-20)

**Tarih:** 20 Nisan 2026, Pazartesi
**Ortam:** Vercel preview (`https://yzliste-8w43bj53i-azizkoses-projects.vercel.app/`)
**Test hesabı:** test@gmail.com / 111111 (24 kredi, 19 üretim)
**Dal:** Otomatik scheduled test (`weekly-comprehensive-test`)

> `www.yzliste.com` Cowork tarayıcısından organizasyon politikasıyla hâlâ engelli. Test Vercel preview üzerinden yapıldı. Preview `x-robots-tag: noindex` başlığı nedeniyle production SEO'dan ayrışabilir — bulgular genel olarak production'a aynen uygular.

---

## Özet Skor Kartı

| Katman | Odak | Durum |
|---|---|---|
| A — Logged-out | Ana sayfa, auth, çerez rıza | 🟢 Geçti (ufak bulgu) |
| A — Logged-in | /hesap/* sayfaları | 🔴 **Kredi tutarsızlığı geri döndü** |
| B — SEO | 14 sayfa metadata, robots, sitemap | 🟡 3 regresyon, çoğu sağlam |
| B — Mobil UX | 375/390/768 viewport | ⚠️ Ortam kısıtı |
| C — İçerik Kalite | 5 ürün × 3 platform × text/image/video | ⚠️ Atlandı (bkz. aşağı) |

**Yeni P0:** 1 (T8-01: kredi tutarsızlığı)
**Yeni P1:** 3 (T8-02, T8-03, T8-04)
**Yeni P2:** 2 (T8-05, T8-06)
**Devam eden açık:** T5-07 (hreflang homepage), T7-04/05 (legal layout), UX-14 (header "Giriş Yap" logged-in)

---

## 🔴 P0 — Kredi Tutarsızlığı (T8-01 / T7-02 REGRESYON)

**Kanıt (aynı anda, aynı oturum, test@gmail.com):**

| Yer | Kredi gösterimi |
|---|---|
| Site header (`SiteHeader`) | 💳 **24 kredi** ✓ |
| `/hesap/profil` — "Kalan kredi" kartı | **24** ✓ |
| `/hesap/krediler` — "Mevcut Kredi" kutusu | **0** ✗ |
| `/uret` sidebar | 24 (Tur 7'de doğrulandı) |

**Sonuç:** Tur 7'de açılan T7-02 teknik olarak kapatılmamış. `/hesap/profil` artık gerçek krediyi gösteriyor (Tur 7'de 0'dı), fakat `/hesap/krediler` sayfası hâlâ 0 gösteriyor. Muhtemelen `/hesap/krediler` farklı bir veri kaynağından/fetcher'dan (SSR'da auth token yok, ya da stale query) besleniyor.

**İş etkisi:** Kullanıcı "Kredi Al →" butonuna bakıp hiç kredisi olmadığını sanarak satın alma başlatabilir veya tam tersi, 0 görünce ürününü tamamlayamayacağını düşünüp kaçabilir. Ödeme ekranı öncesi en kritik sayfada güven kıran hata.

**Yapılacak:** `/hesap/krediler` data-fetching'ini `useCredits()` hook'una veya `/hesap/profil`'in kullandığı aynı kaynağa bağla. Tek kaynak (single source of truth) olacak şekilde refactor.

---

## 🟡 P1 — Yeni Bulgular

### T8-02 — `/hesap/e-postalar` 404
Tur 7 memo'sunda referans verilen `/hesap/e-postalar` sayfası artık yok (HTTP 404, H1 "Sayfa bulunamadı"). Eğer kaldırıldıysa ana hesap navigasyonundan ve sitemap'tan da temizlenmeli; eğer yeniden planlandıysa geri yerleştirilmeli. BACKLOG'a netleştirme: "kaldırıldı mı, taşındı mı?"

### T8-03 — Hesap sayfaları `<title>` çift marka
- `/hesap/profil` → `Profil | yzliste | yzliste`
- `/hesap/ayarlar` → `Ayarlar | yzliste | yzliste`

Sebep büyük ihtimalle parent layout + child metadata title template'i iki kere ekliyor (Next.js metadata API'de `title.template` + `title.default` veya `title` string çakışması). `/hesap/krediler` doğru (`Krediler | yzliste`) — oradan kıyasla.

### T8-04 — 8 sayfada `og:image` yok
og:image eksik olan sayfalar (sosyal paylaşımda kartsız görünür):
- `/giris`, `/kayit`, `/sss`, `/gizlilik`, `/kosullar`, `/hakkimizda`, `/kvkk-aydinlatma`, `/cerez-politikasi`

og:image olan sayfalar: `/`, `/blog`, `/fiyatlar`, `/mesafeli-satis`, `/teslimat-iade` (bu ikisi zaten noindex ama yine de og-image.png).

**Düzeltme:** Root `layout.tsx` veya metadata default'una `openGraph.images = [{ url: '/og-image.png', ... }]` koy — tüm sayfalar miras alır, gereksiz olanlar `openGraph.images = []` ile override etsin.

---

## 🟠 P2 — Kalıntı Regresyonlar

### T8-05 — T5-07 hreflang regresyonu HÂLÂ açık
BACKLOG'da ✅ işaretli ama gerçekte eksik:
- `/` → hreflang `tr` yok, `x-default` yok
- `/mesafeli-satis` → yok (noindex olduğu için kritik değil ama tutarsız)
- `/teslimat-iade` → yok (aynı)

Diğer tüm sayfalarda hreflang `tr` ve `x-default` doğru. **Homepage kritik — en çok çekilen URL.**

### T8-06 — `/uret` H1 yok
`/uret` sayfasında `<h1>` etiketi hiç yok (h1Count: 0). Engine sayfası olsa da SEO ve a11y için semantic heading gerekli (en az 1 `<h1>`). Örn: `<h1 className="sr-only">İçerik Üret</h1>` yeterli.

---

## ✅ Doğrulanan (iyi durumda olan)

- **Kullanıcı kimlik doğrulama akışı** — `/giris` formu test@gmail.com / 111111 ile başarılı giriş yapıp `/uret`'e yönlendirdi.
- **Çerez rıza banner'ı** — vanilla-cookieconsent v3 doğru render oluyor. Türkçe, üç seçenek (Tümünü kabul / Sadece zorunlu / Tercihlerimi ayarla). "Sadece zorunlu" tıklaması `cc_cookie` set'i yaptı, analytics/marketing boş.
- **Header auth-aware** — Logged-in durumda "💳 24 kredi | Hesabım | Çıkış" gösteriyor. (UX-14 başka yerde — hâlâ kontrol gerek.)
- **Sitemap** — 50 URL (10 statik + 40 blog). Tur 7'de yazılan fix çalışıyor.
- **robots.txt** — `/api/`, `/admin`, `/hesap`, `/odeme`, `/giris`, `/kayit` doğru Disallow'da.
- **Güvenlik başlıkları** — `X-Frame-Options: DENY`, HSTS, CSP nonce-based, `X-Content-Type-Options: nosniff` — hepsi aktif.
- **JSON-LD** — 14/14 public sayfada `<script type="application/ld+json">` var.
- **Canonical** — tüm sayfalar production domain'e doğru işaret ediyor. `/giris` ve `/kayit` → `/uret` (auth entry → engine).
- **Title uzunlukları** — çoğu 60 char altında, yalnız "Blog — E-ticaret Listing ve AI Görsel Rehberleri | yzliste" sınırda.
- **Meta description** — benzersiz ve sayfaya özgü, 14/14.

---

## Katman C — Neden atlandı

- Test hesabında 24 kredi var; text=5kr × 3 ürün × 3 platform = 45 kredi gerekir; yetersiz.
- **Tur 6 (19 Nisan)** aynı içerik testini **18 senaryo** ile çalıştırdı (text 4.72, social 3.20). 4 bug (marka bypass, halüsinasyon, social kategori karışması, hashtag kalite) hâlâ BACKLOG'da açık — tekrar koşmak kredi yakıp aynı bulguları üretir.
- **Görsel/video** testi Cowork tarayıcısında file upload çalışmadığı için zaten yapılamıyor (Tur 7 bloker'ı devam ediyor).

**Öneri:** İçerik kalite testi haftalık yerine iki haftalık kadansta kosulsun; bir sonraki Tur 9'da prompt değişiklikleri (PQ-24/25/26) merge edildiyse yapılsın.

---

## Katman B Mobil — Neden sınırlı

`resize_window` tool'u OS pencere boyutunu değiştirse de embedded browser'ın iç render viewport'u 1600×749'da kalıyor (window.innerWidth sabit). Bu ortamda 375/390/768'de görsel doğrulama yapılamıyor. Viewport meta (`width=device-width, initial-scale=1`) doğru set edilmiş, CSS media query'leri mevcut (4 adet media rule stil yaprağında).

**Öneri:** Mobil testler için Aziz'in kendi tarayıcısında DevTools responsive mode ile manuel doğrulama, veya Playwright/Lighthouse CI pipeline'ında otomatize.

---

## Önerilen BACKLOG güncellemeleri

| ID | Öncelik | İş |
|---|---|---|
| T8-01 | P0 | `/hesap/krediler` kredi sayacı tek kaynak — `useCredits` hook'a bağla |
| T8-02 | P1 | `/hesap/e-postalar` 404: kaldır veya geri yükle (nav + sitemap ile tutarlılık) |
| T8-03 | P1 | Hesap sayfaları title "X \| yzliste \| yzliste" çift markayı düzelt |
| T8-04 | P1 | 8 sayfaya og:image ekle (root layout default) |
| T8-05 | P2 | Homepage hreflang regresyonu |
| T8-06 | P2 | `/uret` için sr-only `<h1>` |
| T7-02 | **yeniden açılsın** | Tur 7 T7-02 P0 olarak tekrar açılmalı — T8-01'e dönüşüyor |

---

## Test metodolojisi

- **Katman A logged-out:** Homepage DOM taraması, çerez rıza etkileşimi, form varlık kontrolü, link/img alt denetimi.
- **Katman A logged-in:** 4 hesap route'una SSR fetch + render inspection; sonra `/hesap/profil`, `/hesap/krediler`, `/hesap/ayarlar` client-side render sonrası manuel doğrulama.
- **Katman B SEO:** 14 public URL için HEAD fetch sonrası regex parse: title, canonical, og:title/desc/image, meta description, robots, hreflang (tr + x-default), JSON-LD varlığı.
- **Canlı kontrol yok:** Görsel ekran karşılaştırma/pixel-diff bu ortamda yapılmadı.

---

**Raporu yazan:** Cowork (otomatize scheduled task)
**Takip:** Gmail taslağı azizkose@gmail.com'a oluşturuldu.
